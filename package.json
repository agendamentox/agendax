{
  "name": "agendax-cloudflare-final",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB=agendax-db",
    "deploy": "wrangler pages deploy public --project-name agendax"
  },
  "devDependencies": {"wrangler": "^4.68.0"}
}
